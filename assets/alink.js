/* <a href> tags will be rewritten into <a link> tags, so that the server can know what's going on.
*  This can also improve ability to track the user. */

/*$("a").addEventListener("click", function() {
  s.send(json.send({cmd:}))
  s.onClose = void
})*/
